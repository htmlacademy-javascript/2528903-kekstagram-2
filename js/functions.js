const checkLenght = (string, lenght) => {
  let result = string <= lenght;

  return result;

  /*   if (string <= lenght){
    return true;
  }
  return false; */
}

// Строка меньше указанной длины - true

// Строка больше указанной длины - false
